'use strict'; // Get-info

const urlParams = new URLSearchParams(window.location.search);
const id = urlParams.get('id'); // console.log(id);

fetch('people.json').then(response => response.json()).then(invites => {
  invites.people.forEach(invite => addPeople(invite)); // console.log(`${invite.firstName} ${invite.lastName}.`);
  // const firstName = document.createElement('h4');
  // firstName.innerText = `Förnamn: ${invite.firstName}`;
  // const lastName = document.createElement('h4');
  // lastName.innerText = `Efternamn: ${invite.lastName}`;
}); // if (`${invite.id}` === id) {}
// fetch('people.json')
// .then(response => response.json())
// .then(people => {
// const email = document.createElement('h4');
//   email.innerText = `${people['firstName']}.${people['lastName']}@outlook.com`
//   document.body.appendChild(firstName)
//   document.body.appendChild(lastName)
//   document.body.appendChild(email)
// });
// http://127.0.0.1:5500/index.html?id=0
// Scroll to event

const down = document.querySelector(".scroll-down");
const element = document.querySelector(".event-page");
down.addEventListener("click", () => {
  element.scrollIntoView({
    behavior: 'smooth',
    block: 'start'
  });
}); // Scroll to top

const up = document.querySelector(".scroll-up");
up.addEventListener("click", () => {
  element.scrollIntoView({
    behavior: 'smooth',
    block: 'start'
  });
}); // PREVENT RELOAD ON EVENT-BUTTON + CONFIRMATION

function preventClick(event) {
  event.preventDefault();
}

document.querySelector(".event-button, .expand-button").addEventListener("click", preventClick);

function confirmation() {
  alert('Tack för din anmälan!');
}